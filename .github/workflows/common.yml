on:
  push:
    branches:
      - dev/dev-shrek
      - dev/dev-vikings
      
jobs:
   backup:
    name: Backup ${{ github.head_ref  }}
    environment: ${{ github.ref_name  }} 
    runs-on: ubuntu-latest
    steps:
      - if: branch name is ${{ github.ref_name  }} 
        run:  |
          echo Hello from common workflow
  
